/**
 * Index page generator for GitHub Pages
 * Creates an HTML index listing all generated documents
 */
import { writeFile } from 'fs/promises';
import { join, basename } from 'path';
/**
 * Generate HTML content for the index page
 */
function generateIndexHtml(options) {
    const { title, files } = options;
    const pdfFiles = files.filter((f) => f.type === 'pdf');
    const htmlFiles = files.filter((f) => f.type === 'html');
    const docxFiles = files.filter((f) => f.type === 'docx');
    const renderFileList = (fileList, icon) => {
        if (fileList.length === 0)
            return '<p>No files generated.</p>';
        return `<ul class="file-list">
      ${fileList.map((f) => `<li><a href="${f.name}">${icon} ${f.name}</a></li>`).join('\n      ')}
    </ul>`;
    };
    return `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>${escapeHtml(title)}</title>
  <style>
    :root {
      --primary: #2563eb;
      --primary-hover: #1d4ed8;
      --text: #1f2937;
      --text-muted: #6b7280;
      --bg: #ffffff;
      --bg-secondary: #f9fafb;
      --border: #e5e7eb;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --primary: #60a5fa;
        --primary-hover: #93c5fd;
        --text: #f9fafb;
        --text-muted: #9ca3af;
        --bg: #111827;
        --bg-secondary: #1f2937;
        --border: #374151;
      }
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: var(--text);
      background: var(--bg);
      padding: 2rem;
      max-width: 800px;
      margin: 0 auto;
    }

    header {
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid var(--border);
    }

    h1 {
      font-size: 1.875rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .subtitle {
      color: var(--text-muted);
      font-size: 0.875rem;
    }

    section {
      margin-bottom: 2rem;
    }

    h2 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .file-list {
      list-style: none;
      background: var(--bg-secondary);
      border-radius: 0.5rem;
      border: 1px solid var(--border);
      overflow: hidden;
    }

    .file-list li {
      border-bottom: 1px solid var(--border);
    }

    .file-list li:last-child {
      border-bottom: none;
    }

    .file-list a {
      display: block;
      padding: 0.75rem 1rem;
      color: var(--primary);
      text-decoration: none;
      transition: background 0.15s;
    }

    .file-list a:hover {
      background: var(--border);
      color: var(--primary-hover);
    }

    footer {
      margin-top: 3rem;
      padding-top: 1rem;
      border-top: 1px solid var(--border);
      color: var(--text-muted);
      font-size: 0.75rem;
      text-align: center;
    }

    footer a {
      color: var(--primary);
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>
    <h1>${escapeHtml(title)}</h1>
    <p class="subtitle">Generated on ${new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
  </header>

  ${pdfFiles.length > 0
        ? `<section>
    <h2>📄 PDF Documents (${pdfFiles.length})</h2>
    ${renderFileList(pdfFiles, '📄')}
  </section>`
        : ''}

  ${htmlFiles.length > 0
        ? `<section>
    <h2>🌐 HTML Documents (${htmlFiles.length})</h2>
    ${renderFileList(htmlFiles, '🌐')}
  </section>`
        : ''}

  ${docxFiles.length > 0
        ? `<section>
    <h2>📝 DOCX Documents (${docxFiles.length})</h2>
    ${renderFileList(docxFiles, '📝')}
  </section>`
        : ''}

  <footer>
    Generated by <a href="https://github.com/robinmordasiewicz/yamlforms">yamlforms</a>
  </footer>
</body>
</html>`;
}
/**
 * Escape HTML special characters
 */
function escapeHtml(text) {
    const htmlEscapes = {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#39;',
    };
    return text.replace(/[&<>"']/g, (char) => htmlEscapes[char]);
}
/**
 * Generate the index.html file
 */
export async function generateIndexPage(options) {
    const html = generateIndexHtml(options);
    const indexPath = join(options.outputDir, 'index.html');
    await writeFile(indexPath, html, 'utf-8');
    return indexPath;
}
/**
 * Create GeneratedFile objects from file paths
 */
export function createGeneratedFiles(filePaths) {
    return filePaths.map((path) => {
        const name = basename(path);
        const type = path.endsWith('.pdf') ? 'pdf' : path.endsWith('.docx') ? 'docx' : 'html';
        return { path, type, name };
    });
}
//# sourceMappingURL=index-generator.js.map